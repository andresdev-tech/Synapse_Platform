const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'marianabastiq123@gmail.com' } }); console.log('USER EXISTS:', !!user); } main().finally(() => prisma.$disconnect());
